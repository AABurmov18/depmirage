"""depmirage — spot AI-hallucinated and slopsquatting Python dependencies.

A small, reliable CLI that scans a directory, requirements.txt, or .py file
for four risks: hallucinated (non-existent) packages, typosquat/lookalike
names, hardcoded secrets, and reports them with a clear pass/fail exit code.
"""

__version__ = "0.1.0"
